az-select {
  display: inline-flex;
  position: relative;
  box-sizing: border-box;
  contain: content;
  outline: 0;
  flex: none;
  flex-direction: row;
  margin: 0 .25em 0 0;
  width: auto;
  min-width: 20px;
  border: var(--az-select-border);
  border-radius: 3px;
  cursor: pointer;
  // background-image: linear-gradient(#585858, #444)
}

az-select select {
  font-family: Roboto, -apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'Segoe UI', Oxygen, Ubuntu, Cantarell, 'Open Sans', SourceHanSansCN-Normal, sans-serif;
  -webkit-appearance: none;
  display: flex;
  flex: 1;
  outline: 0;
  border: 0;
  margin: 0;
  padding: .084em 2em .084em .8em;
  // border-top: 1px solid rgba(255, 255, 255, .1);
  // border-bottom: 1px solid rgba(0, 0, 0, .1);
  border-radius: var(--az-select-border-raidus);
  text-decoration: none;
  text-overflow: ellipsis;
  cursor: pointer;
  color: var(--az-button-caption-color);
  background: 0 0;
  width: 100%
}
select option {
  color: var(--az-button-caption-color);
  background: #4e4e4e
}

select optgroup {
  color: var(--az-button-caption-color);
  background: #585858
}

az-select::after {
  display: inline-block;
  position: absolute;
  content: '▼';
  right: 2px;
  top: 1px;
  pointer-events: none;
  width: 1em;
  height: 1em;
  bottom: 0;
  text-align: center;
  transform: scaleY(.6);
}


