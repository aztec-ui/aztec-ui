az-splitter {
  display: flex;
  position: relative;
  padding: 0 !important;
  & > :not(.handle) {
    padding: 0 !important;
    margin: 0 !important;;
    overflow: hidden !important;;
  }
  .handle {
    position: absolute;
    &.active {
      background-color: var(--az-primary-color);
    }
    &:hover {
      background-color: var(--az-primary-color);
      opacity: 0.8;
    }
  }
  &.horizontal{
    flex-direction: row;
    .handle {
      cursor: col-resize;
      top: 0;
      bottom: 0;
    }
  }
  &.vertical {
    flex-direction: column;
    .handle {
      cursor: row-resize;
      left: 0;
      right: 0;
    }
  }
}